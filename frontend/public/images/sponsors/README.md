# Sponsor logos

Drop sponsor logo image files here, then set the sponsor's `logo` field in
`src/data/rodeo.js` to the matching path, e.g. `/images/sponsors/golds-bakery.png`.
Transparent PNG or SVG works best. If `logo` is empty the card stays text-only.
