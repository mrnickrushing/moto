name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Yarn
        run: npm install --global yarn@1.22.22

      - name: Install locked dependencies
        run: yarn install --frozen-lockfile --network-timeout 600000

      - name: Build production frontend
        run: yarn build
        env:
          CI: true
          REACT_APP_BACKEND_URL: ""

  backend:
    name: Backend validation
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements-deploy.txt

      - name: Install production dependencies
        run: python -m pip install -r backend/requirements-deploy.txt

      - name: Compile backend
        run: python -m compileall -q backend

      - name: Import FastAPI application
        run: python -c "import sys; sys.path.insert(0, 'backend'); import server; assert server.app"
        env:
          MONGO_URL: mongodb://127.0.0.1:27017
          DB_NAME: moto_ci
          JWT_SECRET: ci-only-secret-that-is-never-used-outside-this-validation
          FRONTEND_URL: http://localhost:3000
